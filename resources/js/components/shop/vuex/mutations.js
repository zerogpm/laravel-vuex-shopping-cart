export const setProducts = (state, products) => {
    state.products = products
}

// set cart
export const setCarts = (state, cart) => {
    state.cart = cart
};

// clear cart

// remove from cart

// append to cart
